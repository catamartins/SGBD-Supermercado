# Banco-de-Dados-1

## Como executar o backend via terminal:
python -m venv venv -- criação de ambiente virtual

.\venv\Scripts\activate -- ativa ambiente

pip install -r requirements.txt -- instalação das dependencias

python main.py
